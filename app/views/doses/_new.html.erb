<h3 class="border-top pt-5">New Dose:</h3>

<%= simple_form_for [@cocktail, @dose] do |f| %>
<%= f.input :description %>
<%= f.association :ingredient, input_html: { class: "beautiful-dropdown" } %>
<%= f.submit 'Submit', class: "btn btn-primary btn-round" %>
<% end %>
